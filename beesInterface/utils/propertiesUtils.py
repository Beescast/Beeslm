
class PropertiesUtils:

	# Loads properties by given file
	# @ param file filename
	# @ return loaded properties
	# @ throwsjava.io.IOException if can't load file

	@staticmethod
	def load(arg):
		return None

	@staticmethod
	def loadClassLoader(filename):
		return None

	@staticmethod
	def loadAllFromDirectory(dir):
		return None